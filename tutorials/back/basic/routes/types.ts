import { NewPizza, Pizza, PizzaToUpdate } from "../types";

interface Drink {
    id: number;
    title: string;
    image: string;
    volume: number;
    price: number;
  }
  
  export type { Pizza, NewPizza, PizzaToUpdate, Drink };